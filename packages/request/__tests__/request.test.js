'use strict';

/**
 * Resources
 */
const request = require('../src');

/**
 * Unit Tests
 */
describe('@ingresse/request', () => {
    it('needs tests', () => {
        console.log('@ingresse/request', JSON.stringify(request));
    });
});
