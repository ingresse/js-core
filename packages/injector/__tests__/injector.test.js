'use strict';

/**
 * Resources
 */
const injector = require('../src');

/**
 * Unit Tests
 */
describe('@ingresse/injector', () => {
    it('needs tests', () => {
        console.log('@ingresse/injector', JSON.stringify(injector));
    });
});
