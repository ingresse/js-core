/**
 * Install Example
 */
const development = `npm i @ingresse/sdk -D`;
const production  = `npm i @ingresse/sdk isomorphic-unfetch -S`;

/**
 * Exporting
 */
export {
    development,
    production,
};
