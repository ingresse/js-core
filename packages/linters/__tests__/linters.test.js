'use strict';

/**
 * Resources
 */
const linters = require('../src');

/**
 * Unit Tests
 */
describe('@ingresse/linters', () => {
    it('needs tests', () => {
        console.log('@ingresse/linters', JSON.stringify(linters));
    });
});
